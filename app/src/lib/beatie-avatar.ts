import beatieProfileAvatar from "@assets/beatie-profile-avatar.png";

/** Bundled Beatie bot avatar when `profiles.avatar_url` is empty (feed + profile). */
export const BEATIE_PROFILE_AVATAR_SRC: string = beatieProfileAvatar;
