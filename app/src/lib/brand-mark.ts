import brandMarkImage from "@assets/image_1771412400865.png";

/** Bundled URL for the current Diabeaters mark (same asset as `FaceLogo` in the top bar). */
export const BRAND_MARK_IMAGE_SRC: string = brandMarkImage;
